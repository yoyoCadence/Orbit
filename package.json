{
  "name": "orbit",
  "type": "module",
  "scripts": {
    "lint": "eslint pwa/js/**/*.js tests/**/*.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "eslint": "^9.25.0",
    "jsdom": "^29.0.2",
    "vitest": "^1.6.0"
  }
}
