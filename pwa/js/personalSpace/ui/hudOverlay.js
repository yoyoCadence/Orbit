export function renderHudOverlayPlaceholder() {
  return `
    <div class="space-placeholder-block">
      <div class="space-placeholder-title">HUD Overlay</div>
      <div class="space-placeholder-copy">Scene stats, unlock cues, and contextual prompts will mount here.</div>
    </div>
  `;
}
